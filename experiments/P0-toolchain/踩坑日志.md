# 踩坑日志

> P0 唯一的交付物。第二次遇到同一个坑时，这里应该是查表，不是重新受苦。
>
> 记录纪律：
> 1. **报错原文贴最后一行**（错误类型 + 信息），不要转述成「装不上」。
> 2. **「自读时长」这一列如实填** —— 它是 60 秒规则的自我审计。低于 60 秒就问了 AI 的，如实记，别美化。
> 3. **「真实原因」和「解法」是两件事。** 解法是命令，原因是理解。只有解法没有原因的条目，下次还会踩。

## 条目

| 日期 | 仓库 | 报错（最后一行原文） | 自读时长 | 真实原因 | 解法 | 下次怎么更快认出它 |
|---|---|---|---|---|---|---|
| |  |  |  |  |  |  |

<details>
<summary>格式示例（这行不是真实记录，开工后删掉）</summary>

| 日期 | 仓库 | 报错（最后一行原文） | 自读时长 | 真实原因 | 解法 | 下次怎么更快认出它 |
|---|---|---|---|---|---|---|
| 2026-09-02 | micrograd | `ModuleNotFoundError: No module named 'micrograd'` | 90s | 在仓库根目录直接跑脚本，但包没装进当前 venv —— 研究仓库基本都要 editable install | `uv pip install -e .` | 看到 `ModuleNotFoundError` 且模块名 == 仓库名，先查有没有 `-e .`，不要先怀疑 Python 版本 |

</details>

## 复盘（每个仓库跑通后写一行）

| 仓库 | 总耗时 | 坑数 | 最贵的那个坑属于哪一层（环境隔离 / 系统依赖 / 数据 / 显存 / 代码） | 下个仓库我要提前防什么 |
|---|---|---|---|---|
| micrograd |  |  |  |  |
| MuJoCo demo |  |  |  |  |
| LeRobot quickstart |  |  |  |  |

## 坑的分层统计（两周结束时填）

这张表回答一个问题：**我到底弱在哪一层？** L0.5 没过的话，按这里最高的那一格去补。

| 层 | 次数 | 典型症状 |
|---|---|---|
| 环境隔离（venv / 版本 / editable install） |  |  |
| 系统依赖（二进制、渲染后端、驱动） |  |  |
| 数据（路径、下载、格式） |  |  |
| 显存 / GPU |  |  |
| 代码本身（形状、API 变更） |  |  |
