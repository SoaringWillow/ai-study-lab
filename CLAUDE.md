# ai-study-lab — Context for Claude Sessions

## What this repo is

不是软件项目，是一个**学习系统**。仓库的产物是判断力和一串可被外部检验的作品，不是可运行的代码（虽然 `experiments/` 里会有代码）。

主计划在 [PLAN.md](./PLAN.md)。做任何建议前先读它 —— 里面的取舍（学什么、不学什么、怎么验收）都是刻意的。

## Owner background（计划的全部前提）

- 在 **WorldEngine AI**（深圳，2023 成立，Physical AGI 数据平台：大规模机器人部署、数据采集与管理；团队来自自动驾驶 / 云 AI / 机器人）。产业链位置是**数据层**，不是模型层。
- 商业侧职责：面试运营/BD 候选人（北美渠道方向），把「机器人行业认知」当独立评分维度；关心获客、渠道、谈判、单位经济。
- 技术姿态：**定方向和标准，Claude 落地；读 diff 和 review，自己基本不写代码。能读懂基础代码，但写和调的能力不强。** TravelPanel 那套产物（护城河论述、North Star metric、状态机、反模式清单）是在 owner 设定的标准下由 Claude 产出的 —— 证据支持的是「能运维 agent 队伍并对产出验收」，不是「逐字写了代码」。**产品与商业直觉是已有资产；ML/机器人学的形式化训练与「读→跑→改」的工程手感是两个缺口。**
- 中文母语，跨境（中国 ↔ 北美）语境。学习预算：每天 90 分钟 + 周末一个长块。

## 战略（一句话）

不跟人比 GPU。占住**「具身数据与评测」**这个空位 —— 遥操作数据集至今没有公认基准、真机评测普遍不可复现，而 owner 手上有别人拿不到的数据。用公司的数据当**研究护照**，沿「数据 → 策略 → 评测」这条链练到能写出研究者认可的东西。

目标身份：**research-literate operator**，不是全职研究员。按这个标准配资源、按这个标准验收。

## 约定

- **语言**：所有面向 owner 的文档用中文；代码、commit message、目录名用英文。
- **笔记格式**：`notes/YYYY-MM-DD.md`，150–300 字，固定三段「我原以为… 现在知道… 还不懂…」。
- **论文笔记**：`papers/<短名>.md`，必须填满 PLAN.md 第 6 节的 5 问模板（尤其第 5 问「谁的生意会变」）。
- **预测日志**：`predictions/YYYYQn.md`，每条含置信度，季末打分，不许事后改写原文。
- **实验**：`experiments/Pn-<名字>/`，每个实验必须有失败归因表（标定 / 数据 / 策略 / 硬件）。
- **会话日志**：每次会话在 `SESSIONS.md` 追加一条（日期、做了什么、下次从哪继续）。

## Claude 在这个仓库里的角色

允许：出题、挑错、扮演审稿人反驳结论、查资料核对事实、帮搭实验脚手架、帮算单位经济、解释代码和报错。

**不允许**：
- 替 owner 读论文并给结论（前 6 个月）。那正是要练的肌肉。
- 写完代码不让 owner 逐行看懂就推进 —— **逐行可复述原则**：owner 说不出某一行在干什么，这段代码不许 commit。
- 在 owner 自己读 traceback 不足 60 秒时就直接给答案（见 [ENGINEERING.md](./ENGINEERING.md) 第二节）。

分界线：**AI 可以替 owner 打字，不能替 owner 判断。**

## Development branch

主干直接用 `main`；较大的改动用 `claude/<topic>` 分支。
