# 工程手感速查

> 前提：能读懂基础代码，但写和调的肌肉不强。这份文件的目标**不是**把你变成工程师 —— 是把「读 → 跑 → 改」这三个动词练到不再挡路。
> 写代码可以交给 AI。**但有一条铁律：逐行可复述 —— AI 写的任何代码，你要能逐行说出它在干什么，说不出就不许 commit。** 违反这条，12 个月后你会有一堆跑过的脚本和零判断力。

---

## 一、把陌生仓库跑起来（最高频的失败点，不是概念，是这个）

固定套路，每次都一样：

```bash
# 1. 确认 Python 版本：看 README 的 install 段 + pyproject.toml / requirements.txt
# 2. 建隔离环境（uv 是现在的事实标准，比 conda 省事得多）
uv venv --python 3.11
source .venv/bin/activate
# 3. 研究仓库几乎都要 editable install
uv pip install -e .
# 4. 先跑 README 里最小的那个命令 —— 别一上来就跑 full training
```

**每次踩坑都记进 `experiments/P0-toolchain/踩坑日志.md`**：错误信息 → 真实原因 → 解法。第二次遇到就是查表，不是重新受苦。这份日志是 P0 唯一的交付物，也是你未来带团队时最值钱的东西之一。

---

## 二、读 traceback：60 秒规则

**报错先自己读 60 秒再贴给 AI。** 这 60 秒是在练一块别人替你练不了的肌肉。

读法：
1. **从最后一行往上读** —— 最后一行是错误类型和信息，回答「什么坏了」。
2. **往上找第一个属于你的文件路径**（不在 `site-packages/` 里的），回答「哪儿坏了」。
3. 中间那堆库内部的栈帧先全部跳过。

四类占了 90%：

| 报错 | 真实原因 |
|---|---|
| `ModuleNotFoundError` | 环境不对，或者忘了 `pip install -e .` |
| `RuntimeError: mat1 and mat2 shapes cannot be multiplied` | 张量形状 —— 见第三节 |
| `CUDA out of memory` | batch size 太大 / 推理时忘了 `torch.no_grad()` / 显存被别的进程占着（`nvidia-smi` 看一眼） |
| 数据集 `FileNotFoundError` | 数据路径写错，或者数据没下完 |

---

## 三、形状思维（做机器人学习的核心肌肉）

张量出问题，永远先打这三个：

```python
print(x.shape, x.dtype, x.device)
# 或者直接停下来看
breakpoint()
```

机器人策略里的标准形状约定，记住这几个就能读懂大部分代码：

| 张量 | 形状 | 说明 |
|---|---|---|
| 图像 batch | `(B, T, C, H, W)` | B 批量, T 时间步, C 通道 |
| 动作 chunk | `(B, T_chunk, action_dim)` | ACT / π0 一次输出一整段动作 —— **这就是 action chunking 在代码里的样子** |
| 状态 | `(B, state_dim)` | 关节角、夹爪开合等 |

**一次就够、但价值极高的练习**：在 LeRobot 里找到 ACT 的 `forward`，手画一张图 —— 数据从 dataloader 出来后，每一步的形状怎么变，一直到 loss。画完这一张，你才算真的「看懂代码」，而不是「看着眼熟」。

---

## 四、故意制造 bug（这是练读错误，不是练写代码）

把一个 `.permute(0, 2, 1)` 的维度改错、把 batch size 改成不匹配的值、把两个 tensor 的 device 弄成一个 cpu 一个 cuda —— 跑，读报错，自己改回来。**做 3 次。**

这比读 10 篇教程更能建立「报错 → 定位」的反射。而这个反射，是 P2–P5 能不能推进的唯一门槛。

---

## 五、租 GPU 的工作流（三个命令够了）

```bash
ssh -p <port> root@<host>                 # 上机
tmux new -s train                          # 开会话；ctrl-b d 退出，tmux a -t train 回来
rsync -avz ./ root@<host>:/workspace/      # 同步代码上去
```

三条规则：数据放机器上、代码用 git 同步、checkpoint 定期 rsync 回来。**跑长任务永远在 tmux 里** —— 不然网一断，8 小时训练白跑。

---

## 六、AI 结对的边界

| 可以 | 不可以 |
|---|---|
| 让它写脚手架、写画图和数据处理代码 | 让它替你读论文并给结论（前 6 个月） |
| 让它解释一段代码、一个报错 | 让它改完你不看就 commit（违反逐行可复述） |
| 让它出题考你、扮演审稿人反驳你 | 用它跳过第二节的 60 秒规则 |
| 让它帮你算单位经济、查事实 | 让它替你决定实验该测什么 |

分界线很清楚：**AI 可以替你打字，不能替你判断。**

---

## 七、明确不学

OOP 设计模式、类型系统、单元测试框架、打包发布、前端、Docker（除非租的机器要求）、CUDA 编程、分布式训练框架内部。

这些都是真本事，但对「具身数据与评测」这个目标身份是负收益。需要时再回来 —— 或者交给会的人。
